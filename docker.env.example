# ==============================================================================
# OpenSIPS AI Voice Connector - Environment Configuration Template
# ==============================================================================
# Bu dosyayı .env olarak kopyalayın ve değerleri düzenleyin:
# cp docker.env.example .env

# ==============================================================================
# 🌐 NETWORK CONFIGURATION
# ==============================================================================
# Docker Network Settings
# DOCKER_SUBNET=172.20.0.0/16
# DOCKER_GATEWAY=172.20.0.1

# ==============================================================================  
# 🔌 PORT CONFIGURATION
# ==============================================================================
# OpenSIPS Ports
OPENSIPS_SIP_PORT=5060
OPENSIPS_SIP_SECONDARY_PORT=8080
OPENSIPS_MI_PORT=8087
OPENSIPS_EVENT_PORT=8090

# OAVC (Ana uygulama) Ports  
OAVC_SIP_PORT=8089

# AI Service Ports
VOSK_PORT=2700
PIPER_PORT=8000
PIPER_HTTP_PORT=8001
LLM_PORT=8765

# RTP Media Ports
RTP_PORT_MIN=35000
RTP_PORT_MAX=35003
RTP_EXTRA_PORT_1=35010
RTP_EXTRA_PORT_2=35011

# ==============================================================================
# 🤖 AI SERVICES CONFIGURATION  
# ==============================================================================
# LLM Configuration
LLM_MODEL=llama3.2:3b-instruct-turkish
LLM_MAX_TOKENS=80
LLM_TEMPERATURE=0.2
LLM_TOP_P=0.7
LLM_GPU_DEVICE=0

# STT Configuration (Vosk)
VOSK_SAMPLE_RATE=16000

# TTS Configuration (Piper)
PIPER_VOICE_MODEL=tr_TR-dfki-medium
PIPER_SAMPLE_RATE=22050

# ==============================================================================
# ⚙️ APPLICATION CONFIGURATION
# ==============================================================================
# Runtime Settings
TEST_MODE=false
DEBUG_MODE=false
LOG_LEVEL=INFO

# Pipeline Configuration
PIPELINE_MAX_ERRORS=10
PIPELINE_TIMEOUT=30
INTERRUPTION_ENABLED=true

# ==============================================================================
# 📊 LOGGING CONFIGURATION
# ==============================================================================
# Service Log Levels
OPENSIPS_LOG_LEVEL=3
OPENSIPS_LOG_FACILITY=local0
VOSK_LOG_LEVEL=INFO
PIPER_LOG_LEVEL=INFO
LLM_LOG_LEVEL=INFO
EVENT_MONITOR_LOG_LEVEL=INFO

# ==============================================================================
# 🗄️ DATABASE CONFIGURATION (Opsiyonel)
# ==============================================================================
# PostgreSQL Settings
POSTGRES_DB=opensips
POSTGRES_USER=opensips
POSTGRES_PASSWORD=opensips_secure_password_2024

# Redis Settings  
REDIS_PASSWORD=redis_secure_password_2024 

# ==============================================
# 📝 USAGE INSTRUCTIONS
# ==============================================

# 1. Bu dosyayı .env olarak kopyalayın:
#    cp docker.env.example .env

# 2. Değerleri ihtiyacınıza göre düzenleyin

# 3. Docker Compose'u çalıştırın:
#    docker-compose up -d

# 4. Debug/monitoring için:
#    docker-compose --profile debug up -d

# 5. Production için database ile:
#    docker-compose --profile production up -d 